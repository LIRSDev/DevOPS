name: ssh command
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - id: ssh
        uses: invi5H/ssh-action@v1
        with:
          SSH_HOST: 78.47.67.20
          SSH_PORT: 22
          SSH_USER: root
          SSH_KEY: iervasi85
      - run: ssh ${{ steps.ssh.outputs.SERVER }} pwd
